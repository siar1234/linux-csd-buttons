import 'package:flutter/material.dart';

class EditStateStyle extends StatelessWidget {
  const EditStateStyle({super.key});

  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [

      ],
    );
  }
}

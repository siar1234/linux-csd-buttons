import 'package:flutter/material.dart';

class ThemeIcon extends StatelessWidget {
  final String? icon;
  const ThemeIcon({super.key, this.icon});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
